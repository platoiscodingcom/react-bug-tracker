import {PROJECT, TASK} from './types'
import axios from 'axios'

export const createWorkingTimeLog = (workingTime, type) => async dispatch =>{
//unterscheide anhand von type zwischen project und task
}