import React from 'react'

const NewProject = ({category, setShowNewProject, showNewProject, setCategory, match}) => {
  return (
    <div>
      hello new project form
    </div>
  )
}
export default NewProject;
